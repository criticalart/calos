general { 
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'    # to avoid having to press a key twice to turn on the display.
    on_unlock_cmd = calos-restart-waybar                                       # prevent stacking of waybar when waking
    inhibit_sleep = 3                                                          # wait until screen is locked
}

listener {
    timeout = 1200                                                             # 20 minutes
    on-timeout = calos-launch-screensaver                                      # start screensaver 
}

listener {
    timeout = 6000                                                             # 100 minutes!!
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'        # screen off when timeout has passed
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'          # screen on when activity is detected
}
